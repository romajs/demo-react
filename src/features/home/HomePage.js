import React from 'react'
import { Header } from '../misc/Header'

export const HomePage = (props) => (
  <React.Fragment>
    <Header />
    <hr />
    <h1>HomePage</h1>
  </React.Fragment>
)

HomePage.propTypes = {
}
