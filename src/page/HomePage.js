import React from 'react'
import { Header } from '../component/Header'

export const HomePage = (props) => (
  <div>
    <Header />
    <hr />
    <h1>HomePage</h1>
  </div>
)

HomePage.propTypes = {
}
